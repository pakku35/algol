begin comment program to check numbers;
	integer a;
	ininteger(0,a);
	if a>0 then outstring(1,"Positive\n") 
	else 
		begin
		if a=0 then outstring(1,"Zero\n")
		else outstring(1,"Negative\n")
		end

end